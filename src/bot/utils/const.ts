import { config } from "@config";

export const DEFAULT_MINUS_MINUTES = config.defaultMinusMinutes;
export const MESSAGES_LIMIT = config.messagesLimit;
export const EVERY_NTH_MESSAGE = config.everyNthMessage;
export const MIN_MESSAGE_LENGTH = config.minMessageLength;
